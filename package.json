{
  "name": "toucanrender",
  "version": "0.0.2",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel --extensions .js src --out-dir ./dist",
    "test": "echo No test available && exit 0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0"
  }
}
